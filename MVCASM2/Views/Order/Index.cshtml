@model IEnumerable<Order>
<script>
	function deleteConfirm() {
		if (confirm("Are you sure to delete!")) {
			return true;
		} else {
			return false;
		}
	}
</script>
<h2>List Of Order</h2>
<table class="table table-hover">
	@foreach (var order in Model)
	{
		<tr>
			<td>@order.Order_Id</td>
		    <td>@order.Cus_Name</td>
		    <td>@order.DeliveryLocal</td>
		    <td>@order.Cus_Phone</td>
		    <td>@order.OrderDate</td>
		    <td>@order.DeliveryDate</td>
			<td>
				<a asp-action="Edit" asp-route-id="@order.Order_Id">Edit</a>
				<a asp-action="Delete" asp-route-id="@order.Order_Id" onclick="return deleteConfirm()">Delete</a>
			</td>
		</tr>
	}
</table>

@*@foreach (var cat in Model)
{
	<h5 class="text-danger">Id is: @order.Order_Id</h5>
	<p>Hello everyone, my name is <b>@order.Cus_Name</b> description <b>@order.DeliveryDate</b></p>
	<hr />
}*@

